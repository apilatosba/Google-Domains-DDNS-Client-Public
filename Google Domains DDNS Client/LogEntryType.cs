namespace Google_Domains_DDNS_Client {
   // If you add a new entry type, make sure to update the LogEntry.ToString() method.
   internal enum LogEntryType {
      Info,
      Warning,
      Error,
   }
}
